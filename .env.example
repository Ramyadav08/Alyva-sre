# Copy to .env and fill in real values. .env is gitignored — never commit real secrets.

# Shared hackathon LGTM stack — read-only query endpoints (Mimir/Loki/Tempo).
MANAGED_MIMIR_URL=http://10.10.1.139:9009
MANAGED_LOKI_URL=http://10.10.1.139:3100
MANAGED_TEMPO_URL=http://10.10.1.139:3200
MANAGED_LGTM_ORG_ID=hackathon

# Reasoning — this product's own key, used for drafting/tuning alert rules.
OPENAI_API_KEY=

# Web review UI
PORT=4310
